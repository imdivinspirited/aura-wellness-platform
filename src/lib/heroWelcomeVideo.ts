/**
 * YouTube video id for Home “Watch video” (hero modal).
 *
 * Playback is 100% streamed from YouTube’s CDN via an iframe embed — we do not host
 * or store video files on this app; only this short id string ships in the frontend bundle.
 */
export const HERO_WELCOME_YOUTUBE_ID = 'lXKZuUqnTRg';
